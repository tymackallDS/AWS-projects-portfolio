
#latest version: always supposed to be here
#cfn -cloud formation
AWSTemplateFormatVersion: "2010-09-09"

Description: A simple s3 bucket

Resources:
  S3Bucket:
    #https://docs.aws.amazon.com/AWSCloudFormation/latest/UserGuide/aws-resource-s3-bucket.html
    Type: 'AWS::S3::Bucket'
    # We dont need to supply a bucket name as aws will supply one for us